{
  "name": "@earendil-works/inkling-core",
  "version": "0.0.0",
  "description": "Runtime-independent Inkling domain models and application contracts.",
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./domain": "./src/domain/document.ts",
    "./ports": "./src/application/ports.ts"
  },
  "scripts": {
    "build:package": "rm -rf dist && tsc -p tsconfig.build.json && node ../../scripts/fix-declaration-imports.mjs dist",
    "prepublishOnly": "echo 'Publish from the repository root with pnpm release' >&2 && exit 1",
    "test": "node --test test/*.test.ts",
    "typecheck": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "effect": "3.22.1"
  }
}
