{
  "name": "@earendil-works/inkling-renderer",
  "version": "0.0.1",
  "description": "Deterministic safe Markdown rendering for Inkling.",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build:package": "rm -rf dist && tsc -p tsconfig.build.json && node ../../scripts/fix-declaration-imports.mjs dist",
    "prepublishOnly": "echo 'Publish from the repository root with pnpm release' >&2 && exit 1",
    "test": "node --test test/*.test.ts",
    "typecheck": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@codemirror/language": "6.12.4",
    "@codemirror/language-data": "6.5.2",
    "@lezer/highlight": "1.2.3",
    "effect": "3.22.1",
    "markdown-it": "15.0.0",
    "yaml": "2.8.2"
  }
}
